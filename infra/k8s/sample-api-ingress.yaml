apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sample-api-ingress
  namespace: autoops
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: sample.autoops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sample-api
                port:
                  number: 80
