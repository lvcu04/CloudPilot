apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: autoops
spec:
  selector:
    app: sample-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
